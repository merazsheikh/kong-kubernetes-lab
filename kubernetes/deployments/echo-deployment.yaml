apiVersion: apps/v1
kind: Deployment

metadata:
  name: echo
  namespace: kong-learning
  labels:
    app: echo

spec:
  replicas: 2

  selector:
    matchLabels:
      app: echo

  template:
    metadata:
      labels:
        app: echo

    spec:
      containers:
        - name: echo
          image: mendhak/http-https-echo:latest

          ports:
            - containerPort: 8080

          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"

            limits:
              cpu: "100m"
              memory: "64Mi"
